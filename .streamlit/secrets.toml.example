# Copy this file's contents into Streamlit Cloud's "Secrets" panel,
# or into a local .streamlit/secrets.toml (which is gitignored).
# NEVER commit a real secrets.toml file.

GEMINI_API_KEY = "your_gemini_api_key_here"
